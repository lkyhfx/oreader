{
"version": "0.2.0",

  "configurations": [
    {
      "command": "npm run dev",
      "name": "Launch Program",
      "request": "launch",
      "type": "chrome",
      "cwd": "${workspaceFolder}"
    },
    {
      "name": "Attach by Process ID",
      "processId": "${command:PickProcess}",
      "request": "attach",
      "skipFiles": ["<node_internals>/**"],
      "type": "node"
    }
  ]
}